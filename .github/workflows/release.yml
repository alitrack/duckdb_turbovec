name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true

  build:
    needs: create-release
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux_amd64
            lib: target/release/libturbovec.so
            deps: sudo apt-get update && sudo apt-get install -y libopenblas-dev
          - os: macos-latest
            platform: osx_arm64
            lib: target/release/libturbovec.dylib
            deps: "true"
          - os: windows-latest
            platform: windows_amd64
            lib: target/release/turbovec.dll
            deps: "true"

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        shell: bash
        run: ${{ matrix.deps }}

      - name: Extract version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - uses: dtolnay/rust-toolchain@stable

      - name: Build release
        run: cargo build --release

      - name: Generate extension
        id: generate
        shell: bash
        run: |
          EXT_FILE="turbovec-${{ matrix.platform }}.duckdb_extension"
          python3 scripts/metadata.py \
            --extension-version "${{ steps.version.outputs.VERSION }}" \
            ${{ matrix.lib }} \
            -o "$EXT_FILE"
          echo "ext_file=$EXT_FILE" >> $GITHUB_OUTPUT

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: ${{ steps.generate.outputs.ext_file }}
