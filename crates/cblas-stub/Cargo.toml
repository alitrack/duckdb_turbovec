[package]
name = "cblas-sys"
version = "0.1.4"
edition = "2021"
description = "Pure-Rust drop-in replacement for cblas-sys using matrixmultiply"
license = "MIT"

[lib]
name = "cblas_sys"
crate-type = ["rlib"]

[dependencies]
matrixmultiply = "0.3"
